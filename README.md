Install (ohmyz.sh)[https://ohmyz.sh/]

```
git clone https://github.com/fedeaux/configuration
ln -s configuration/.spacemacs .spacemacs
```

add `source /Users/fedorius/configuration/.zshrc_ext` to `~/.zshrc`.
